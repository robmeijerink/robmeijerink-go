FROM golang:1.26-alpine

WORKDIR /app

# Install Air for hot-reloading
RUN go install github.com/air-verse/air@latest

# Cache Go modules first
COPY go.mod go.sum ./
RUN go mod download

# Start Air using the config file
CMD ["air", "-c", ".air.toml"]
